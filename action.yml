name : "adr4901 Action"
description: 'Greets someone and returns always 42.'
inputs:
  who-to-great: # id of the input
    description: 'Who to greet'
    required: true
    default: 'World'

outputs:
  answer: #id of the output
    description: 'The answer to everythin (always 42)'

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
   - ${{ inputs.who-to-greet }}
   
